{
  "name": "pogglo",
  "version": "0.2.0",
  "description": "Publish AI-made browser games to pogglo from your terminal: npx pogglo publish",
  "type": "module",
  "bin": {
    "pogglo": "bin/pogglo.js"
  },
  "files": [
    "bin/",
    "src/"
  ],
  "scripts": {
    "test": "node --test",
    "prepare": "git config core.hooksPath .githooks || exit 0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "adm-zip": "^0.5.16"
  }
}
